% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmodel.R
\name{mmodel}
\alias{mmodel}
\title{Fit Multivariable Model}
\usage{
mmodel(
  data,
  outcome,
  predictors,
  model_type = "glm",
  family = "binomial",
  interaction_terms = NULL,
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table containing the analysis dataset.}

\item{outcome}{Character string specifying the outcome variable. For survival 
analysis, use Surv() syntax, e.g., "Surv(time, status)".}

\item{predictors}{Character vector of predictor variable names.}

\item{model_type}{Character string specifying model type. Options: "glm", "lm", 
"coxph", "clogit", "coxme", "glmer". Default is "glm".}

\item{family}{For GLM models, the error distribution family. Default is "binomial". 
See \code{\link[stats]{family}} for options.}

\item{interaction_terms}{Character vector of interaction terms using colon syntax, 
e.g., c("age:sex", "bmi:smoking"). Default is NULL.}

\item{strata}{For Cox/conditional logistic models, variable name for stratification. 
Default is NULL.}

\item{cluster}{For Cox models, variable name for robust clustered standard errors. 
Default is NULL.}

\item{weights}{Character string naming the weights variable in data. Default is NULL.}

\item{...}{Additional arguments passed to the underlying model fitting function.}
}
\value{
A fitted model object with additional attributes:
\itemize{
  \item \code{formula_str}: Character. The formula used as a string
  \item \code{predictors}: Character vector. The predictor variables
  \item \code{model_type}: Character. The type of model fitted
  \item \code{interaction_terms}: Character vector. Interaction terms (if any)
  \item \code{data_name}: Character. Name of the data object used
}
}
\description{
A unified interface for fitting various types of regression models with 
consistent syntax across different model types (GLM, Cox, mixed effects, etc.)
}
